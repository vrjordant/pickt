<section> 
	<a href = "/profile">Profile</a>
	<a href = "/logout">Logout</a>
	<br>
	<a href = "/feed/local">Local</a>
	<a href = "/feed/state">State</a>
	<a href = "/feed/regional">Regional</a>
	<a href = "/feed/national">National</a>
	<h6>PIKT</h6>
	<p>You are logged in as {{username}}</p>

	{{#if disabled}}
	<p>You are out of votes for this round!</p>
	{{/if}}

	<p>{{location}}</p>

	{{#if visible}}
	<form id = "formID" action="/feed/" enctype = "multipart/form-data" method="POST">
		<label>{{formLabel}} </label>
		<input id = "picture" type="file" name="pic" accept="image/*">
  		<input type="submit">
	</form>
	{{/if}}

	<div class="form-group hidden" id="error-container">
        <div class="alert alert-danger text-goes-here">
        </div>
    </div>
	<div>
    <p>Taken from wikpedia</p>
	{{#each posts}}
		<article class = "post">
			<img src="data:image/png;base64, {{data}}" alt="Picc" />
			<form id = "upvoteID" action="/feed/upvote" method="POST">
				<input type="hidden" id="picID" name="picID" value="{{_id}}">
				{{#unless ../disabled}}
				<input type="submit" value="Upvote">
				{{/unless}}
			</form>
		</article>
	{{/each}}
</div> 
</section>